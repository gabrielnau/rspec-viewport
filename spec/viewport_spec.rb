require File.expand_path("spec_helper.rb", File.dirname(__FILE__))

describe RSpec::Viewport, type: :feature do

  pending "checks a driver that supports viewport resize"
  pending "implements a resize method for each driver"

  describe "#viewport" do

    pending "wraps given block in a nested example group"

  end

end